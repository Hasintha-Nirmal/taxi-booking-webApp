# Backend API Configuration
# Base URL for the backend API server
# Development: http://localhost:3000
# Production: https://your-production-api-url.com
VITE_BASE_URL=http://localhost:3000

# Note: There's an inconsistency in the codebase - some files use VITE_API_URL
# Make sure to set both to the same value for now
VITE_API_URL=http://localhost:3000

# Google Maps API Configuration
# Get your API key from: https://console.cloud.google.com/google/maps-apis
# Required APIs to enable:
# - Maps JavaScript API (for rendering maps)
# - Places API (for location search and autocomplete)
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here
